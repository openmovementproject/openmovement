%% Takes input meta-data from a FileInfo Object read from a Device
%% and enters it into a existing structure.

function SetMeta(input,output)
{
    %some code goes here to overwrite output's existing parameters with
    %those of inputs.
    
}